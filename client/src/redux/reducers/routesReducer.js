

const initialState = {
    bookmarks: "/bookmarks",
    todos: "/",
    auth: "/auth",
}

const routesReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default routesReducer